# Runtime dependencies for the Streamlit app.
# Kept in sync with the root requirements.txt — deploy either, not both.
streamlit>=1.31.0
numpy>=1.23.0
faiss-cpu>=1.7.4
rank-bm25>=0.2.2
openai>=1.99.0
requests>=2.32.0
python-dotenv>=1.0.0
