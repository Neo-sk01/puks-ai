# ===========================================
# Secrets — never commit these
# ===========================================
*secrets.toml
.streamlit/secrets.toml
.env
.env.*
!.env.example
*.pem
*.key

# ===========================================
# Python
# ===========================================
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# Acceptance verdicts and runtime state (SQLite, temp files) — local, never committed
/var/

# ===========================================
# Node / Next.js  (for the rebuild)
# ===========================================
node_modules/
.next/
out/
dist/
*.tsbuildinfo

# ===========================================
# Generated retrieval artefacts
# Rebuild these from SCRIPTS/ rather than committing them.
# ===========================================
indexes/
build/
*.npy
*.f32

# ===========================================
# OS / editor
# ===========================================
.DS_Store
desktop.ini
Thumbs.db
.vscode/
.idea/

# Internal — tenant identifiers, access chain, network posture. Never commit.
ENVIRONMENT.local.md

# Next.js
web/node_modules/
web/.next/
web/out/
web/.env.local

# evals/error-discovery — derived from docs/ + corpus by prepare.py; reviewer files stay tracked
evals/error-discovery/data/records.json
evals/error-discovery/data/graph.json
.playwright-mcp/
